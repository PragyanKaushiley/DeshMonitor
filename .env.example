# Neon PostgreSQL
DATABASE_URL=

# Upstash Redis
UPSTASH_REDIS_REST_URL=
UPSTASH_REDIS_REST_TOKEN=

# Web app (apps/web/.env.local locally; a build-time variable on Cloudflare).
# Base URL of apps/api — visit recording is skipped when unset.
NEXT_PUBLIC_API_URL=http://localhost:8787
