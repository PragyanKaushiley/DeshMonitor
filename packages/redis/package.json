{
  "name": "@desh-monitor/redis",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "echo \"no lint configured for @desh-monitor/redis\"",
    "test": "vitest run"
  },
  "dependencies": {
    "@desh-monitor/config": "workspace:*",
    "@upstash/redis": "^1.39.0"
  },
  "devDependencies": {
    "vitest": "^2.1.8"
  }
}
